def foo()->void:{

    a:int:=555, i:float;
    b:float;

    b := 1.1<2 && 1<3*0.4;
    print(b);
    for i in range(0.1, 5.5, 1.1):{
    
    
        if(i < 4):{
        
            tmp:int:=1;
        
            print(a, i);
        } else: {
        
        
            tmp:=2;
            while (b < 7):{
            
            
                print(b);
                b := b+1.1;
            }
            return;
        }
    }
}

def main()->void:{


    foo();
}